# Assets

These assets are included for repository presentation and future UI work.

- `gopota-wordmark.*` — compact GoPoTa wordmark.
- `gopota-character-crown.*` — panel background character/crown artwork.

The current userscript embeds UI images as data URIs, so these files are not required at runtime.

Publish these assets only if you have the rights to use them publicly.
